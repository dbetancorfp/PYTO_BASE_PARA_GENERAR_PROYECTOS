# No [test].preload yet — src/frontend and src/backend don't exist (no vista implemented
# yet). Add a [test] section with preload = ["./src/frontend/tests/dom-setup.ts",
# "./src/backend/tests/setup.ts"] once implementer creates those files for the first vista.
